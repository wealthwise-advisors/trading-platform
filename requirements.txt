# Production runtime dependencies -- mirrors [project.dependencies] in
# pyproject.toml. Prefer `pip install -e .` (or `-e ".[dev]"` for
# development); this file exists for environments (Docker, simple venvs)
# that want a flat pip-install without editable/package metadata.
#
# Audited in Task 10 (v1.0.0 release audit) against actual `import`
# statements across the whole repo -- not assumed. Corrected two real gaps
# (fastapi/uvicorn/pydantic/openpyxl/reportlab/python-docx were used but
# undeclared) and dropped two stale entries (streamlit, scipy -- zero
# remaining imports anywhere; the Streamlit UI was fully removed, see
# CLAUDE.md). See docs/RELEASE_AUDIT.md for the full dependency audit.

# Core data
pandas>=2.0.0
numpy>=1.24.0

# pyarrow is pandas' parquet engine, and api/store.py writes saved backtest
# results as parquet. pandas only pulls it in under its [parquet] extra, which
# a bare `pandas>=2.0.0` does not install -- so this must be declared here or
# a clean environment silently loses persistence: to_parquet raises, store.py
# catches it by design, and results are never written. It was present during
# development only as a leftover of streamlit, which was removed on 2026-07-15.
pyarrow>=13.0.0   # >=13 matches pandas' own floor for the parquet extra

# Password hashing for the login system (argon2id). api/auth.py stores the
# full encoded hash, so the cost parameters can be raised later without
# invalidating existing passwords.
argon2-cffi>=23.1.0

# Technical indicators
pandas_ta>=0.4.71b0

# Visualization / reporting
plotly>=5.15.0

# Config & logging
pyyaml>=6.0
loguru>=0.7.0
python-dotenv>=1.0.0

# Schwab market data API
requests>=2.31.0
websockets>=11.0

# FastAPI backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# Report export (api/export/formats.py: CSV/Excel/PDF/Word)
openpyxl>=3.1.0
reportlab>=4.0.0
python-docx>=1.1.0
Pillow>=12.3.0   # security pin -- see pyproject.toml comment (pip-audit, PYSEC-2026-2253 et al.)

# Optional extras (not installed by default -- see pyproject.toml):
#   pip install -e ".[live]"   Rithmic real/live market data (requires a licensed account)
#   pip install -e ".[dev]"    pytest, ruff, mypy, vulture, bandit, pip-audit, black
